# restful-booker-api-framework
QA automation framework for Restful Booker Platform — manual test design (EP/BVA), API test automation (pytest + requests), and SQL-level verification (H2) across a Dockerized microservices system.
